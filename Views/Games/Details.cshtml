@model Game
@{
    ViewData["Title"] = Model.Name;
}

<div class="card rounded-4 mt-5 overflow-hidden" > 
    <div class="card-body p-0">
        <div class="row">
            <div class="col-md-8"> </div>
            <div class="col-md-4">
                <img src="@($"{FileSettings.ImagePath}/{Model.Cover}")" alt="Model.Name" class="w-100"/>
            </div>


           
        </div>
    </div>
</div>

